# Fountain View Hall

## Using uv

Follow the [uv install instructions](https://docs.astral.sh/uv/getting-started/installation/) and run:

```cmd
uv sync
```

run with:

```cmd
uv run fvh-manager
```
